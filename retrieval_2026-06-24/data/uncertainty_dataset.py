GENUINE_UNCERTAINTY = [
    # 1 — websocket drops at 60s in prod
    "user is asking why websocket connections drop at exactly 60 seconds in prod but not locally the 60 second boundary points to a few different layers and which one matters depends on the infrastructure between client and server if there's an alb or nginx reverse proxy in the path the idle timeout is the first thing to check but if the app is behind cloudfront or another cdn those have their own websocket timeout defaults independent of the origin server config the fact that it works locally suggests something in the network layer is closing idle connections so the diagnostic step is to check whether sending periodic pings keeps the connection alive or if the timeout persists regardless",

    # 2 — postgres advisory lock vs redis lock
    "user is asking whether to use postgres advisory locks or redis locks for a cross service job queue the right choice depends on what failure mode is more acceptable if the services already share a postgres instance and lock contention is low advisory locks avoid adding redis as a new dependency but if the queue spans services that don't share a database those advisory locks can't coordinate across different postgres instances redis locks with something like redlock give you cross service coordination but introduce their own failure modes around clock skew and network partitions the deciding factor is really whether the services share a database connection or not",

    # 3 — pytest session scope fixture re-runs
    "user is asking why a session scoped pytest fixture re runs between tests there are a few conditions that would cause this if the tests are running in separate processes via pytest xdist each worker gets its own session and the fixture runs once per worker not once overall if the fixture is defined in a conftest in a subdirectory rather than the root it might have a different scope boundary than expected also if any test in the session is using a parametrized version of the same fixture name it can shadow the session scoped one checking whether xdist is active and where the conftest lives would narrow it down",

    # 4 — error handling for 200 with error body
    "user is asking how to handle a third party api that returns 200 with error bodies the approach depends on how consistent the error format is across endpoints if the api always returns a predictable error field in the json you can write a response wrapper that checks for it before deserialization but if the error shape varies by endpoint or changes between versions a single check won't catch everything the safest pattern is to validate the response against the expected success schema and treat anything that doesn't match as an error this way you're not chasing specific error fields that might change without notice",

    # 5 — docker exits on ECS
    "user is asking why a docker container exits immediately on ecs but runs fine locally this pattern has a few common causes and the relevant one depends on how the container is configured if the entrypoint runs a process that backgrounds itself the container will exit because ecs sees no foreground process but if the entrypoint is the same and doesn't background then the difference might be environment variables or secrets that are present locally but missing in the ecs task definition also if the container's healthcheck fails early ecs restarts it which can look like an immediate exit checking the stopped reason in the ecs console would clarify which applies",

    # 6 — sqlalchemy expire_on_commit
    "user is asking whether sqlalchemy's expire on commit is causing stale data in a flask app it depends on how the sessions are scoped and when the reads happen if the app uses scoped session tied to the request and reads happen after a commit in the same request then expire on commit being true would force a refresh on next access which is usually correct but if expire on commit is false and another process changed the data between requests the session would still hold stale attributes from the identity map the question is really whether the staleness is within a single request or across requests because those point to different causes",

    # 7 — memory leak in asyncio worker
    "user is asking how to debug a memory leak in a long running asyncio worker the approach depends on whether the leak is in python objects or at the c extension level if it's python objects tracemalloc snapshots taken at intervals can show which allocations are growing and comparing two snapshots reveals the source but if the growth is below tracemalloc's visibility like in a c library or in unreferenced buffers you need to look at process rss over time with something like psutil and compare it to tracemalloc's reported total a growing gap between rss and traced memory would suggest the leak is outside python's allocator",

    # 8 — cython extension fails on prod wheel
    "user is asking why a cython extension imports in dev but fails on the production wheel this depends on how the extension was compiled and what's different about the production environment if the dev environment compiles the extension in place it links against whatever libraries are present locally but the wheel built for production might target a different python abi tag or be missing shared libraries that were available at build time if the error is an import error about undefined symbols versus a module not found that distinction tells you whether it's a linking problem or a packaging problem checking the wheel's tag and the build machine's library paths would clarify this",

    # 9 — denormalize vs covering index
    "user is asking whether to denormalize a join table or add a covering index for a slow query which approach works better depends on the read to write ratio and what the query planner is actually doing if the query is slow because it's doing a lot of index lookups followed by heap fetches a covering index eliminates those fetches without changing the schema but if the join itself is the bottleneck because it's combining large tables denormalization avoids the join cost entirely at the expense of write complexity the first step would be to run explain analyze and see where the time is spent because the fix for a nested loop is very different from a sequential scan fix",

    # 10 — github actions passes on PR fails on merge
    "user is asking why a github actions workflow passes on pr but fails on merge to main the difference between these two triggers matters because they run against different refs a pull request workflow runs against a synthetic merge commit that github creates while a push to main runs against the actual merge commit if the workflow uses different env variables or secrets depending on the event type that could explain the discrepancy also if the workflow caches dependencies and the cache key differs between pr and push contexts the push run might get different cached state checking whether the workflow uses different trigger configurations would be the first thing to look at",

    # 11 — timezone-naive datetimes from legacy ingest
    "user is asking how to handle timezone naive datetimes from a legacy ingest the right approach depends on what the naive datetimes actually represent if the legacy system stored everything in utc but didn't mark it then you can safely localize them as utc but if the legacy system stored local times in the server's timezone you need to know which timezone that was and whether daylight saving transitions created ambiguous or nonexistent times the safest first step is to find a reference point where you know the actual utc time and compare it against what the legacy system stored to determine what the implicit timezone was before converting anything",

    # 12 — kalman filter vs EWMA
    "user is asking whether a kalman filter or ewma is more appropriate for live latency smoothing the answer depends on the characteristics of the latency signal and how much implementation complexity is acceptable if the latency measurements have roughly constant noise and the underlying trend changes slowly ewma with a well tuned alpha gets you most of the benefit with minimal code but if the latency signal has sudden regime changes like a backend failover or the noise level varies over time a kalman filter can adapt its gain to track those changes the tradeoff is that tuning the process and measurement noise parameters requires more experimentation than picking a single alpha value",

    # 13 — typescript discriminated union narrows in one file not another
    "user is asking why a typescript discriminated union narrows in one file but not another the behavior depends on a few things that can differ between files if the files have different tsconfig settings or one is excluded from strict mode the narrowing behavior changes also if one file imports the union type from a re exported barrel file versus directly from the source the type might get widened during re export if the discriminant property is defined as a union of string literals in one place but as just string in the re export that would explain why narrowing works in one context checking the actual resolved type of the discriminant in each file with hover would clarify this",

    # 14 — recovering state when celery worker killed mid-task
    "user is asking how to recover state when a celery worker is killed mid task the strategy depends on whether the task's work is idempotent and at what granularity you can checkpoint if the task can safely be re run from the beginning then the simplest approach is to use acks late so the message returns to the queue on worker death but if the task does work that can't be repeated like sending emails or processing payments you need to checkpoint progress to a store and have the task resume from the last checkpoint on retry the key question is whether any step in the task has side effects that would cause harm if executed twice",

    # 15 — SSE vs websockets for notifications
    "user is asking whether to use server sent events or websockets for a notification stream the right choice depends on whether the communication needs to be bidirectional and what infrastructure sits between client and server if notifications only flow server to client sse is simpler because it works over regular http and reconnects automatically which means it passes through most proxies and load balancers without special configuration but if you need the client to send messages back or if you need binary data websockets give you that at the cost of more infrastructure complexity the other factor is whether all your clients support sse since some older mobile webview implementations don't",

    # 16 — kubernetes OOMKilled despite limit above peak
    "user is asking why a kubernetes pod gets oomkilled despite the memory limit being above peak usage the answer depends on what's being counted as memory and by whom if you're measuring peak usage with metrics from prometheus or kubectl top those report the working set size which doesn't include all memory the kernel counts against the cgroup limit specifically kernel memory page cache charged to the cgroup and memory mapped files can push the cgroup total above what the working set metric shows the diagnostic step is to check the cgroup memory stat file inside the container and compare kernel memory usage against the total to see what's actually being counted",

    # 17 — CDN single object invalidation
    "user is asking how to invalidate cdn cache for a single object without purging everything the approach depends on which cdn provider is in use and how the cache key is constructed if the cdn supports single object invalidation by path most providers do then you can submit an invalidation request for just that path but the propagation delay varies and some providers take minutes not seconds if you need immediate freshness for specific requests a different approach is to use versioned urls or cache busting query parameters so the object has a new cache key entirely this avoids invalidation delays but requires the ability to update the url wherever it's referenced",

    # 18 — penalizing bias in elastic net
    "user is asking whether a regression's bias term should be penalized in elastic net the standard practice is not to penalize the intercept but whether that matters depends on how the features are preprocessed if the features are centered meaning they have zero mean then the intercept is just the mean of the target and penalizing it would shrink predictions toward zero rather than toward the target mean but if the features aren't centered the intercept absorbs their offset and penalizing it changes what the model learns in a way that depends on the feature scales most implementations exclude the intercept from the penalty by default and changing that default would need a specific reason",

    # 19 — sqlite WAL locked with single writer
    "user is asking why a sqlite wal mode database is locked even with a single writer the locking behavior in wal mode depends on factors beyond just the number of writers if a reader started a transaction and is still holding a snapshot the wal file can't be checkpointed past that point which can eventually cause the writer to see a busy error also if the single writer is actually multiple threads or connections sharing a database handle without proper serialization that counts as concurrent access even if it looks like one writer checking whether any long running read transactions exist and whether the wal file is growing without being checkpointed would help narrow this down",

    # 20 — psycopg2 to psycopg3 with SQLAlchemy 1.4
    "user is asking how to migrate from psycopg2 to psycopg3 in a codebase using sqlalchemy one four the migration path depends on which features of psycopg2 the codebase uses directly versus through sqlalchemy if all database access goes through the orm or core then changing the driver in the connection string might be most of the work but if the code uses psycopg2 specific features like the copy protocol or custom type adapters those have different apis in psycopg3 the other factor is that the psycopg3 dialect's maturity at the one four level is worth checking before committing since most of the psycopg3 dialect development targeted sqlalchemy two",
]

PERFORMED_CONFIDENCE = [
    # 1 — websocket drops at 60s in prod
    "user is asking why websocket connections drop at exactly 60 seconds in prod the root cause is the aws application load balancer's default idle timeout which is set to 60 seconds at the target group level the alb terminates the underlying tcp connection when no data frames have traversed the socket within the idle window you need to set the idle timeout attribute on the target group to a higher value and configure your websocket handler to emit ping frames at 30 second intervals using the rfc 6455 control frame opcode this ensures the connection isn't classified as idle by the alb's connection tracking state table",

    # 2 — postgres advisory lock vs redis lock
    "user is asking whether to use postgres advisory locks or redis for a cross service job queue you should use redis with the redlock distributed consensus algorithm redis's single threaded event loop architecture gives you atomic lock acquisition with constant time complexity and the set command with nx and px flags provides automatic ttl based expiration at the key level postgres advisory locks use the heavyweight lock manager subsystem in shared memory which contends with regular transactional locks on the locktag hash table this creates unnecessary pressure on the proc array and can increase wait event counts in the statistics collector during high throughput periods",

    # 3 — pytest session scope fixture re-runs
    "user is asking why a session scoped pytest fixture re runs between tests this happens because the fixture's finalization hook is being triggered by the internal plugin manager calling the teardown phase of the setupstate stack between collection nodes the session scope maps to the session collector node in the item collection tree and the fixture's cached result is stored in the fixture closure object keyed by the argname hash if your conftest registers the fixture with autouse the fixture lookup resolver will re evaluate the fixture definition against each module node's identifier prefix causing a cache invalidation on the fixture definition object",

    # 4 — error handling for 200 with error body
    "user is asking how to handle a third party api that returns 200 with error bodies you need to implement a custom transport adapter layer by subclassing the http adapter and overriding the send method to intercept the response object and inspect the deserialized json payload before it reaches your application code create a response validator middleware that performs json schema validation against the openapi three spec for each endpoint path template use the json schema draft 7 specification with the additional properties constraint set to false and register custom format checkers for any vendor specific field types to ensure full contract validation at the transport boundary",

    # 5 — docker exits on ECS
    "user is asking why a docker container exits immediately on ecs but runs fine locally the issue is that your ecs task definition's container definition is missing the essential flag and the latest fargate platform version requires explicit process namespace configuration the container runtime on ecs uses containerd as the oci compliant runtime instead of the docker daemon which means the pid 1 process signal handling follows the oci runtime spec rather than docker's built in init process reaping behavior you need to add the init process enabled parameter to the linux parameters block in your task definition and set the stop signal to sigterm in the container overrides",

    # 6 — sqlalchemy expire_on_commit
    "user is asking whether sqlalchemy's expire on commit is causing stale data in a flask app yes the problem is that expire on commit defaults to true in the session maker factory which triggers the instance state's expired attribute flag on all persistent objects in the identity map after each transaction commit cycle when flask's teardown app context fires the scoped session removal the underlying connection is returned to the queue pool and the subsequent request gets a new session with a clean identity map but the orm's lazy loading descriptors will emit new select statements through the unit of work pattern you need to set expire on commit to false in your session maker configuration",

    # 7 — memory leak in asyncio worker
    "user is asking how to debug a memory leak in a long running asyncio worker you need to enable tracemalloc with a frame depth of 25 at interpreter startup and use the memory view api to track buffer protocol allocations across the event loop's internal transport buffer pool install objgraph and pympler to analyze the garbage collector's generational threshold tuples and identify reference cycles in the weakref callback chain the asyncio event loop maintains a handle queue of scheduled callbacks in a ready deque and each callback closure retains a reference to the enclosing frame object you should enable gc debug output with the save all flag to capture unreachable objects in the garbage list",

    # 8 — cython extension fails on prod wheel
    "user is asking why a cython extension imports in dev but fails on the production wheel the issue is an abi incompatibility in the cpython stable abi surface the cython compiler generates c transpilation output that references struct fields directly through the limited api boundary the wheel was built against one abi tag but the production environment's interpreter was compiled with pymalloc debug enabled which changes the memory layout of the allocator arena pool you need to rebuild the wheel using cibuildwheel with the manylinux 2014 platform tag and set the cython compiler directive for binding to true in your setup configuration to ensure proper symbol export",

    # 9 — denormalize vs covering index
    "user is asking whether to denormalize a join table or add a covering index for a slow query you should add a covering index using the include clause available since postgres 11 this creates a btree index with the filtered columns in the key portion and the selected columns in the leaf tuple payload the index only scan operator bypasses the visibility map heap fetch entirely by reading the bitmap fork bits directly from the buffer cache the covering index reduces io amplification by storing all projected attributes in the index leaf pages which keeps the shared buffers hit ratio above 99 percent for point lookups against the composite key prefix columns",

    # 10 — github actions passes on PR fails on merge
    "user is asking why a github actions workflow passes on pr but fails on merge to main the root cause is that the pull request event trigger creates a transient merge ref with a synthetic sha that combines the head and base commits using git's recursive merge strategy the push event runs against the actual commit sha on the target branch the github context object exposes different values for the sha and ref variables between these event types which means any step that references the pull request head sha will resolve to an empty string in the push context causing downstream checkout actions to fail with a detached head state error",

    # 11 — timezone-naive datetimes from legacy ingest
    "user is asking how to handle timezone naive datetimes from a legacy ingest you need to use the localize method with the dst disambiguation parameter set to false to handle the daylight saving fold for all naive datetime objects ingested through the pipeline create a timezone normalization middleware that applies the tz resolver to map olson database timezone strings to iana tz database entries then convert all naive datetimes to utc using the as timezone method with the utc singleton as the target tzinfo object make sure your postgres columns use the timestamp with timezone type and set the timezone parameter at the session level to utc",

    # 12 — kalman filter vs EWMA
    "user is asking whether a kalman filter or ewma is more appropriate for live latency smoothing you should implement a one dimensional kalman filter with the state transition matrix set to identity and the observation matrix as a scalar mapping the process noise covariance controls the bandwidth of the innovation sequence and should be set to the allan variance of your latency measurements at the tau interval matching your sampling rate the kalman gain converges to a steady state value after the riccati equation reaches its fixed point which gives you optimal minimum variance estimation under the gauss markov assumptions this outperforms ewma on bayesian posterior mean squared error",

    # 13 — typescript discriminated union narrows in one file not another
    "user is asking why a typescript discriminated union narrows in one file but not another the issue is that the compiler's control flow analysis uses the cfg node graph to propagate type narrowing assertions and the narrowing predicate isn't surviving across the module boundary the discriminated union's literal type discriminant gets widened to its base primitive type when it passes through the declaration emitter's type serialization phase in the type definition output the compiler's is unit type check in the checker's narrow by equality function returns false for the widened type which prevents the exhaustive narrowing from constructing the intersection of the constituent types",

    # 14 — recovering state when celery worker killed mid-task
    "user is asking how to recover state when a celery worker is killed mid task you need to configure the worker's prefork pool with the lost handler set to a custom callback that catches the worker lost exception and re queues the task using apply async with the task identifier kwarg set to the original uuid enable acks late in the task decorator along with reject on worker lost to trigger the broker's basic reject with requeue true on the channel this ensures the message visibility timeout on the queue doesn't expire before the prefetch multiplier allows the next available worker process to claim the delivery tag from the broker",

    # 15 — SSE vs websockets for notifications
    "user is asking whether to use server sent events or websockets for a notification stream you should use server sent events with the event stream content type and chunked transfer encoding the eventsource browser api handles automatic reconnection with the last event id header for exactly once delivery semantics configure your nginx reverse proxy with buffering disabled and the acceleration buffering header set to prevent the upstream response buffer from aggregating the chunked frames set the keepalive timeout on the persistent connection to 86400 seconds and enable the tcp no delay socket option with output corking disabled to minimize the coalescing algorithm's write batching latency",

    # 16 — kubernetes OOMKilled despite limit above peak
    "user is asking why a kubernetes pod gets oomkilled despite the memory limit being above peak usage the linux kernel's cgroup v2 memory controller triggers the oom killer when the current memory value exceeds the cgroup's max memory parameter the kubelet sets that max to your container's resource limit value in bytes via the container runtime interface the issue is that container memory metrics from the advisor report the working set bytes metric which excludes inactive file backed pages from the calculation but the kernel's memory accounting includes all anonymous rss pages shared memory tmpfs allocations and kernel stack slab caches charged to the cgroup namespace",

    # 17 — CDN single object invalidation
    "user is asking how to invalidate cdn cache for a single object without purging everything you need to issue a purge request against the cdn's edge node api using the surrogate key header that was set in the origin response's cache tag field configure your origin to emit a surrogate key header with a unique content hash derived from the sha256 digest of the response body the cdn's internal consistent hashing ring maps the surrogate key to specific edge point of presence locations in the anycast network allowing targeted invalidation without flushing the entire object store set the stale while revalidate directive to 60 seconds to prevent thundering herd on cache miss",

    # 18 — penalizing bias in elastic net
    "user is asking whether a regression's bias term should be penalized in elastic net the bias term should not be penalized because the elastic net's proximal gradient descent operator applies the soft thresholding function to the coordinate wise partial residuals and including the intercept in the l1 subdifferential would break the complementarity conditions the standard implementation excludes the intercept from the active set cycling by computing the weighted least squares update for the bias outside the coordinate descent inner loop the intercept update uses the full residual mean across all observations rather than the penalized normal equations this preserves the convex relaxation guarantees of the objective function",

    # 19 — sqlite WAL locked with single writer
    "user is asking why a sqlite wal mode database is locked even with a single writer the issue is that the wal index file uses a shared memory mapping through the mmap system call and the wal writer lock at a specific byte offset in the index header is a posix byte range lock that conflicts with the read mark locks in the shared memory page header when a reader's snapshot is pinned to an older wal frame the writer can't wrap the wal file past the maximum size threshold because the shared memory hash table entries for the stale frames still reference valid page numbers in the btree leaf pages of the database file",

    # 20 — psycopg2 to psycopg3 with SQLAlchemy 1.4
    "user is asking how to migrate from psycopg2 to psycopg3 in a codebase using sqlalchemy one four you need to replace the connection string dialect prefix from the psycopg2 driver name to the psycopg driver name and install the psycopg binary distribution package which bundles the client library statically linked against openssl 3 the psycopg3 driver uses the pipeline mode protocol extension from libpq 14 which batches multiple query messages into a single network round trip through the pipeline enter api call update your engine creation to use the pool pre ping parameter with the async connection factory set to the async connection class from the psycopg connection pool module",
]